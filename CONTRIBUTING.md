# Contributing

Welcome! To add a new RL algorithm:

1. Create `src/agents/<algo_name>.py`
2. Add tests in `tests/test_<algo_name>.py`
3. Log results in `docs/EXPERIMENTS.md`
4. Update the comparison table in README.md
5. Open a PR!
